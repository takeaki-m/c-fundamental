#include <stdio.h>

int main (int argc, char *argv[]) 
{
    char character;
    int number;

    // printf に %cを指定すると、数値を文字として表示できる
    printf("charcter is = %c\n", character);
    printf("number is = %d\n", number);
    return 0;
} 